# HabitTracker

Простой трекер привычек, созданный с помощью Angular. Помогает следить за ежедневными задачами и поддерживать стрик.

## Текущие возможности

На данный момент HabitTracker способен:

- Отображать список всех привычек.
- Показывать текущий стрик (подряд идущие дни выполнения).
- Отмечать дни выполнения привычки в недельном и месячном календаре.
- Добавлять и удалять привычки.
- Сохранять прогресс в `localStorage`.

> 💡 Визуальный стиль: фиолетовый градиент, звёздный фон, овальные ячейки.

## Запуск

Склонируйте репозиторий и запустите локальный сервер с помощью:

ng serve


После запуска откройте в браузере: `http://localhost:4200`

## Детали реализации

Проект использует следующие технологии и подходы:

- Angular CLI — для создания, сборки и управления проектом.
- LocalStorage — для хранения данных без сервера.
- RxJS — для реактивного обновления интерфейса при изменении данных.
- Material Design — компоненты (`mat-card`, `mat-button`) для базового UI.
- SCSS — для стилизации с градиентами, анимациями и адаптивностью.

⚠️ Проект не использует Angular Material полностью — только базовые компоненты. Основной стиль написан вручную.

## Как пользоваться

- Введите название привычки и описание.
- Нажмите «Добавить привычку».
- В календаре кликайте по дням, чтобы отметить выполнение.
- Следите за стриком — он обновляется автоматически!
